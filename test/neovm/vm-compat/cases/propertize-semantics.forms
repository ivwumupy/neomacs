; propertize argument validation and return-shape semantics.

(propertize "abc")

(equal (propertize "abc" 'face 'bold) "abc")

(stringp (propertize "abc" 1 'x))

(equal (propertize "abc" 1 'x) "abc")

(condition-case err
    (propertize 1 'face 'bold)
  (error err))

(condition-case err
    (propertize "abc" 'face)
  (error err))
